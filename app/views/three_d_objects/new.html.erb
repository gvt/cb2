<h1>ThreeDObjects#new</h1>

<p>Upload a new 3D object in the .obj format</p>

<%= form_for :object, :url => three_d_objects_path, :html => { :method => :post, :multipart => true } do |form| %>
	
	<%= form.file_field :object %>
	<br/>
	<%= form.submit('Save')%>
	
<%- end %>
